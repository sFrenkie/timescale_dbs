# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'pg', '= 1.1.4' # cannot compile pg after that version

gem 'rubocop'
gem 'solargraph'
